# Optional live mode. Static demo mode does not require these values.
# Must be exactly "enabled" before the server will use OPENAI_API_KEY.
IMPACT_REPORTER_LIVE_AI=
OPENAI_API_KEY=
OPENAI_MODEL=

# Keep public hosted demos synthetic-only unless explicitly approved.
IMPACT_REPORTER_DEMO_MODE=static
